<%- if @plants.empty? %>
  <p class='no-plants-message'>
    You have no plants yet. Add a plant!
  </p>
<%- else %>
  <ul>
    <% @plants.each do |plant| %>
      <li><%= link_to "#{plant.name}", plant_path(plant) %></li>
    <% end %>
  </ul>

<% end %>

<%= link_to "Add Plant", new_plant_path %>
